import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
// import 'package:firebase_core/firebase_core.dart';

void main() {
  // Ensure Firebase is initialized before tests run
  // setUpAll(() async {
  //   WidgetsFlutterBinding.ensureInitialized();
  //   await Firebase.initializeApp();
  // });

  testWidgets('UI Test: Check if UI widgets are present', (WidgetTester tester) async {
    // Your UI test code here
  });

  testWidgets('Adding and Deleting Event Test', (WidgetTester tester) async {
    // Your test code here
  });
}

